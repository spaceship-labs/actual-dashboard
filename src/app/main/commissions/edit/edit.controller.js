(function ()
{
    'use strict';

    angular
        .module('app.commissions.edit')
        .controller('CommissionsEditController', CommissionsEditController);

    /** @ngInject */
    function CommissionsEditController($mdDialog, $stateParams, userService, api, dialogService){
        var vm = this;
    }
})();
